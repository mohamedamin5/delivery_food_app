rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // تم فتح الصلاحيات بالكامل للتطوير والتدريب المحلي بدون قيود زمنية
      allow read, write: if true;
    }
  }
}